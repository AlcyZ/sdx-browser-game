{
  "scripts": {
    "dev": "npx snowpack dev",
    "build": "npx snowpack build",
    "build:wasm": "wasm-pack build --target web --out-dir assets/wasm"
  },
  "devDependencies": {
    "@snowpack/plugin-sass": "^1.4.0",
    "@snowpack/plugin-typescript": "^1.2.1",
    "snowpack": "^3.1.2",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "@types/gl-matrix": "^3.2.0",
    "gl-matrix": "^3.3.0"
  }
}
